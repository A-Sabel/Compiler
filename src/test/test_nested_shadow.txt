class NestedShadowTest {
    void method() {
        int value = 10;
        println(value);
        
        if (true) {
            int value = 20;  // Shadows outer value in if block
            println(value);
        }
    }
}
